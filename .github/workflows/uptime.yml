name: Uptime Monitor
on:
  schedule:
    - cron: '0 * * * *' # Hourly
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Check Live Portal
        run: curl -fIs https://itsrkmahapatra.github.io/${{ github.event.repository.name }}/ || exit 1
